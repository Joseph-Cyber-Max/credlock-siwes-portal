import {useState} from 'react';
import {UserPlus,ShieldCheck} from 'lucide-react';
import {APPS_SCRIPT_URL as API} from '../../config';
import './Auth.css';
export default function Registration(){const[form,setForm]=useState({fullName:'',email:'',phone:'',institution:'',programme:'',level:'',organization:'',password:'',confirmPassword:''}),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState('');const change=e=>setForm({...form,[e.target.name]:e.target.value});async function submit(e){e.preventDefault();setError('');setMessage('');if(form.password!==form.confirmPassword)return setError('Passwords do not match.');if(form.password.length<6)return setError('Password must be at least 6 characters.');setBusy(true);try{const r=await fetch(API,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action:'register',payload:form})});const j=await r.json();if(!j.success)throw Error(j.message||'Registration failed.');setMessage(j.message||'Registration submitted successfully.')}catch(x){setError(x.message)}finally{setBusy(false)}}return <div className="auth-page"><div className="auth-brand"><div className="brand-mark">C</div><div><b>Credlock</b><small>SIWES Portal</small></div></div><form className="auth-card" onSubmit={submit}><span className="eyebrow">STUDENT REGISTRATION</span><h1>Create your portal account</h1><p>Register your SIWES profile. Your account will be activated after verification.</p><div className="form-grid">{[['fullName','Full name','text'],['email','Email','email'],['phone','Phone','tel'],['institution','Institution','text'],['programme','Programme','text'],['level','Level','text'],['organization','SIWES organization','text'],['password','Password','password'],['confirmPassword','Confirm password','password']].map(([n,l,t])=><label key={n}>{l}<input name={n} type={t} value={form[n]} onChange={change} required/></label>)}</div>{error&&<div className="error">{error}</div>}{message&&<div className="success">{message}</div>}<button className="primary wide" disabled={busy}><UserPlus size={17}/>{busy?'Submitting…':'Register'}</button><div className="auth-foot"><ShieldCheck size={15}/> Registration is handled by the portal backend.</div><p className="auth-link">Already registered? <a href="#/login">Sign in</a></p></form></div>}
