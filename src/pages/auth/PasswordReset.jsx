import {useState} from 'react';
import {KeyRound,ShieldCheck} from 'lucide-react';
import './Auth.css';
const API=import.meta.env.VITE_APPS_SCRIPT_URL||'https://script.google.com/macros/s/11dg34n-gM_uhow87okjURvKmtapYZrTejeg6xnSSPf8kRHnWtIPLmC0S/exec';
export default function PasswordReset(){const[email,setEmail]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState('');async function submit(e){e.preventDefault();setBusy(true);setError('');setMessage('');try{const r=await fetch(API,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action:'requestPasswordReset',payload:{email:email.trim()}})});const j=await r.json();if(!j.success)throw Error(j.message||'Unable to process request.');setMessage(j.message||'If the account exists, reset instructions have been prepared.')}catch(x){setError(x.message)}finally{setBusy(false)}}return <div className="auth-page"><div className="auth-brand"><div className="brand-mark">C</div><div><b>Credlock</b><small>SIWES Portal</small></div></div><form className="auth-card compact" onSubmit={submit}><span className="eyebrow">ACCOUNT RECOVERY</span><h1>Reset your password</h1><p>Enter the email address associated with your SIWES account. The backend will prepare a temporary reset token.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label>{error&&<div className="error">{error}</div>}{message&&<div className="success">{message}</div>}<button className="primary wide" disabled={busy}><KeyRound size={17}/>{busy?'Processing…':'Request password reset'}</button><div className="auth-foot"><ShieldCheck size={15}/> Reset requests are processed by the portal backend.</div><p className="auth-link"><a href="#/login">Back to sign in</a></p></form></div>}
